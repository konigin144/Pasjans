#include "game.hpp"

//case sf::Event::MouseButtonPressed:
//if (event.mouseButton.button == sf::Mouse::Left)

//void move(VectorCards pileFrom, VectorCards pileTo, int highestCard) {
	//sf::Vector2i mouseStartPosition = sf::Mouse::getPosition(window);
	
//}

//bool canMove(int pileFrom, int pileTo, int numOfCards)

//bool Game::continueGame() {
	/*if (board.getDiamondsSize() == 13 && board.getClubsSize() == 13 && board.getHeartsSize() == 13 && board.getSpadesSize() == 13)
		return false;
	else return true;*/
	//return true;
//}