#pragma once
#include "libraries.hpp"

class Game {
//	Board board;
public:
	Game() {}
	//bool canMove(int pileFrom, int pileTo, int numOfCards);
	//void move(VectorCards pileFrom, VectorCards pileTo, int highestCard);
	//void goBack();
	//bool continueGame();
};